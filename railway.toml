[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn BudgetAppBackend.app:app --bind 0.0.0.0:$PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10